"use client";

// import { useRouter } from "next/router";

export default function PostDescription({ title }: { title: string }) {
  // const router = useRouter();
  // console.log(router.query.slug);
  return <div>Test</div>;
}
